version: '3.9'

services:
  servers-stats:
    container_name: servers-stats
    build: .
    restart: unless-stopped
    ports:
      - "1324:1324"
    volumes:
      - /:/host:ro
      - ./logs:/app/logs
